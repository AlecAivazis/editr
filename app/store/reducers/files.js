// the initial state
const state = {
    total: [],
    active: []
}

// this reducer manages the file of files visible to the user
export default (state=state, ({type, payload}) => {

    return state
}
